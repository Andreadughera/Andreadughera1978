# DollarSpot Sentinel v5.2 - Validation log

## 2026-05-19

### Ambiente Google Sheets TEST

Da screenshot:

- Spreadsheet `DollarSpot Sentinel v5.2 TEST` creato.
- Fogli presenti:
  - `Campi`
  - `Audit`
  - `Errori`
  - `MeteoCache`
  - `Registro`
  - `Monitor`
  - `Storico`
- `MeteoCache` popolata con dati multi-campo.
- Header `Campi` esistenti in formato leggibile, ad esempio:
  - `Nome Campo`
  - `Latitudine`
  - `Longitudine`
  - `Profilo`
  - `Feltro 1-9`

### Web App TEST

Endpoint health verificato:

```json
{
  "status": "ok",
  "service": "DollarSpot Sentinel v5.2 TEST",
  "mode": "test"
}
```

Nota: l'URL della Web App TEST non viene committato in questo repository per evitare di fissare endpoint operativi nei sorgenti.

### Osservazioni

- Il setup e stato aggiornato per accettare gli header leggibili del foglio `Campi`.
- La verifica POST da terminale non e affidabile per il redirect di Google Apps Script; i test POST vanno eseguiti da pagina HTML v5.2 o da funzione Apps Script dedicata.

### Prossimi test manuali

1. Eseguire `validateDollarSpotV52Environment()`.
2. Verificare che `Errori` sia vuoto o non contenga errori bloccanti.
3. Testare un salvataggio `annotazione` con token valido da frontend.
4. Testare un salvataggio con token errato e verificare rifiuto.
5. Testare salvataggio `rullatura`.
6. Testare azoto con data futura e verificare rifiuto.
